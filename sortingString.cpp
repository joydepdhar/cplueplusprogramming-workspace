#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin>>s;
    cout<<s<<endl;
    // sort(starting, ending);
    sort(s.begin(),s.end());
    cout<<s<<endl;

    return 0;
}